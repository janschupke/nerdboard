// DEPRECATED: Use the unified storage manager from src/services/storageManager.ts instead.
export class SmartStorageManager {
  constructor() {
    throw new Error(
      'SmartStorageManager is deprecated. Use the unified storage manager from src/services/storageManager.ts instead.',
    );
  }
}
export const smartStorage = undefined;
