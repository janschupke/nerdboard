// DEPRECATED: Use the unified storage manager from src/services/storageManager.ts instead.
export class SidebarStorageServiceImpl {
  constructor() {
    throw new Error(
      'SidebarStorageServiceImpl is deprecated. Use the unified storage manager from src/services/storageManager.ts instead.',
    );
  }
}
export const sidebarStorage = undefined;
