import type { TileCategory } from '../../../types/tileCategories';
import type { TileMeta } from '../../tile/GenericTile';
export const federalFundsRateTileMeta: TileMeta = { title: 'Federal Funds Rate', icon: 'chart', category: 'Macroeconomics' as TileCategory };
export const meta = federalFundsRateTileMeta;
